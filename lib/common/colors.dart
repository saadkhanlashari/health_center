import 'package:flutter/material.dart';

const primaryColor = Color(0xff05468A);
const notifiyColor = Color(0xff668C25);
const gradinatColor = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: <Color>[
      Color.fromARGB(255, 99, 142, 189),
      primaryColor,
    ]);
